package com.example.codexiabeta.ui.theme

import androidx.compose.ui.graphics.Color

// Primary Palette
val ArcaneIndigo = Color(0xFF0D1B2A) // Deep, dark blue for backgrounds
val Parchment = Color(0xFFFDF6E3)     // Warm, off-white for paper/cards
val BrilliantGold = Color(0xFFFFD700) // Accent for highlights and interactive elements
val MutedGray = Color(0xFF8D8D8D)      // For secondary text and icons

// Supporting Colors
val DeepBlue = Color(0xFF1B263B)      // Slightly lighter background variant
val SubtleGlow = Color(0x99FFD700)    // Semi-transparent gold for glows
val AccentOrange = Color(0xFFE27D3E)